---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import Carousel from '../../components/Carousel.astro';
import ProcessOverview from '../../components/ProcessOverview.astro';

const { entry } = Astro.props;
const { Content } = await entry.render();

export async function getStaticPaths() {
  const portfolioEntries = await getCollection('portfolio');
  return  portfolioEntries.map(entry => ({
    params: { slug: entry.slug }, props: { entry },
  }));
}


---
<Layout title={entry.data.title} description={entry.data.description}>
	<main class="main portfolio-main" slot="main">
   
    <div class="portfolio-text-content">
      
      <div id="portfolio-category-container">
        {entry.data.category.map((item) => (
        <span class="portfolio-category">{item}</span>
        ))}
      </div>
      <div id="portfolio-name-container">
        <h1 class="portfolio-name">{entry.data.title}</h1>
      </div>
    </div>
    {entry.data.threeDModel && (
      <div id="portfolio-thumbnail-container" class="portfolio-image-container">
       
          <>
          <script is:raw type="module" src="https://unpkg.com/@splinetool/viewer@1.9.6/build/spline-viewer.js"></script>
          <spline-viewer url={entry.data.threeDModel}></spline-viewer>
          </>

      </div>
 )}
    <section id="portfolio-intro-section">
        <div id="portfolio-intro-container">
          <div id="portfolio-intro-content" class="portfolio-sub-section">
              <h2 class="portfolio-subsection-headers">Introduction</h2>
              {entry.data.introduction.map((copy) => (

              <p class="portfolio-intro-text">{copy}</p>
              ))}
            {entry.data.website && (
              <>
              <a class="button primary-button" href={entry.data.website}>View Website</a>
              </>
              )}
          
            </div>

          <div id="portfolio-time-content" class="portfolio-sub-section">
             
              <h3>Project Duration</h3>
              <p>{entry.data.projectDuration}</p>
            </div>
            <div id="portfolio-objective-content" class="portfolio-sub-section">
              <h3>Objective</h3>
              {entry.data.projectGoals.map((goal) => (

              <p>{goal}</p>
              ))}
            </div>

            <div id="portfolio-role-content" class="portfolio-sub-section">
             
              <h3>My Role</h3>
              <ul>
              {entry.data.roles.map((role) => (

                <li class="portfolio-intro-text">{role}</li>
                ))}
                </ul>
            </div>

            {entry.data.team && (
              <>
            <div id="portfolio-team-content" class="portfolio-sub-section">
             
              <h3>Team Members</h3>
                <ul>
                  {entry.data.team?.map((member) => (
                    <li>{member.name} - {member.position}</li>
                  ))}
                  </ul>
            </div>
            </>
            )}
      </div> 

         

     
    </section>
    <section>
      <h2 class="portfolio-subsection-headers">Product Overview</h2>

      <div id="portfolio-overview">

      <div id="portfolio-challenge" class="portfolio-sub-section">
          <img class="portfolio-icons" src="/images/portfolio/challenge.svg"/>
          <h2 class="portfolio-subheaders">Challenge</h2>
          
          <ul class="portfolio-list">
            {entry.data.challenges?.map((challenge) => (
              <li><input type="checkbox" disabled="disabled" /> {challenge}</li>
            ))}
          </ul>
      </div> 

      <div id="portfolio-solution" class="portfolio-sub-section">
        <img class="portfolio-icons" src="/images/portfolio/solution.svg"/>
        <h2 class="portfolio-subheaders">Solution</h2>
        
    
          {entry.data.solutions?.map((solution) => (
            <p>{solution}</p>
          ))}
      </div> 

      <div id="portfolio-results" class="portfolio-sub-section">
        <img class="portfolio-icons" src="/images/portfolio/results.svg"/>
        <h2 class="portfolio-subheaders portfolio-results-header">Results</h2>
          {entry.data.results?.map((result) => (
            <p>{result}</p>
          ))}

          {entry.data.impact?.map((item) => (
          <div class="portfolio-impact">
            <h3 class="portfolio-impact-header">{item.header} </h3>
            <p class="portfolio-impact-description">{item.description}</p>
          </div>
          ))}

      </div> 
    </div>
      
    </section>
    <ProcessOverview processes={entry.data.processes.map(process => ({ 
            src: process.src.src,
            header: process.header,
            activities: process.activities

            }))}  />
          <Carousel header="Screenshots" 
            images={entry.data.carousel.map(image => ({ 
              src: image.src.src, 
              caption: image.caption || ''
            }))} 
          />
       
          <!-- {entry.data.impact && (
            <>
            <ColumnTextBlock header="Product Impact"  content={entry.data.impact.map(item => ({ header: item.header, description: item.description, list: item.list || [] }))} />
            </>
                                   
                                   )} -->

          
          <!-- <Content /> -->
      
          </div>
	</main>
</Layout>
<style lang="scss">

#portfolio-intro-container{
      padding:2rem;
    
    }

  .primary-button{
    color:white;
    text-decoration: none;
    &:hover{
      color:white;
    }
  }
  section{
    margin-top: 4rem;
    margin-bottom: 4rem;

    &:first-child{
      margin-top: 0;
    }
  }

  .portfolio-icons{
    max-height: 64px;
    padding: 1rem;
    
  }
  .portfolio-category{
    background: #333;
    color:white;
    text-transform: uppercase;
    padding: 0.25rem 0.5rem;
    border-radius: 1rem;
    margin: 0;
    font-weight: 700;
    font-size: 0.75rem;
    margin-bottom: 2rem;
    margin-right: 0.5rem;
  }

  .portfolio-name{
      padding-top:1rem;
      padding-bottom: 2rem;
    }

  .portfolio-header {
      font-size: 1.75rem;
      margin: 0;
      padding: 0;
      padding: 1rem 0;
      text-transform: uppercase;
  }

  .portfolio-list{
    list-style-type: none;
    padding: 0;

  }

  .portfolio-image-container {
    width: 100%;
    height: 100%;

    img {
      margin: 0;
      padding: 0;
      height: auto;
      width: 100%;
      border-radius: 1rem;

    }
  }

  .portfolio-text-content{
    padding:0;
    margin: 0;
    padding: 1rem;
  }

  .portfolio-sub-section{
    padding: 0;
    margin: 0;
    background:white;
    border-radius: 1rem;
    margin-top: 1rem;

    padding: 1.5rem ;
    box-shadow: 0 0 1rem 1rem rgba(128,128,128,0.1);
  }


  #portfolio-challenge{
      display: flex;
      flex-direction: column;
      justify-content: start;
      align-items: center;
      background: white;
      box-shadow: 0 0 1rem 1rem rgba(128,128,128,0.1);
      h2{
        color:black;
        text-align:center;
      }
      li{
        display: flex;
        justify-content: start;
        align-items: center;
        gap: 1rem;
        padding: 0;
        margin: 0;
        padding-bottom: 1rem;

        vertical-align: middle;
      }

      ul{
        list-style-type: none;
        padding: 0;
        margin: 0;
      }
      
    }

    #portfolio-solution{

      
display: flex;
flex-direction: column;
justify-content: start;
align-items: center;
background-color: white;
box-shadow: 0 0 1rem 1rem rgba(128,128,128,0.1);


h2{
  color:black;
  text-align:center;
}
li{
  padding: 0;
  margin: 0;
}

ul{
  list-style-type: none;
  padding: 0;
  margin: 0;
  padding: 1rem;
}
}

#portfolio-results{

display: flex;
flex-direction: column;
justify-content: start;
align-items: center;
background: white;
box-shadow: 0 0 1rem 1rem rgba(128,128,128,0.1);


h2{
  color:black;
  text-align:center;
}
li{
  padding: 0;
  margin: 0;
}
}



  @media only screen and (min-width: 768px) {
    #portfolio-thumbnail-container{
      width: 100%;
      small {
        display:block;
        text-align: center;
        width: 100%;
      }
    }

    .portfolio-image-container {


    img {
      margin: 0;
      padding: 0;
      width: 100%;
      border-radius: 1rem;

    }
  }

  .portfolio-impact{
    display: flex;
    justify-content: start;
    align-items: center;
    flex-direction: row;
  }
    .portfolio-impact-header{
      display:inline;
      width: 40%;

      padding: 1rem;
      text-align: center;
    }
    .portfolio-impact-description{
      display:inline;
      width: 60%;
      padding: 1rem;
    }


    .portfolio-text-content{
  }

  .portfolio-subheaders{
    font-size: 1.25rem;
    width: 100%;
  }
    .portfolio-name{
      font-size: 4rem;
    }
  

    #portfolio-intro-container{
      display: grid;
      grid-template-columns: repeat(6, 1fr);
      grid-template-rows: 50% 50%;
      gap: 2rem;
      padding-bottom: 4rem;
      h2{
        text-align: left;
      }
    
    }

    
    #portfolio-intro-content{
      grid-column: 1 / 4;
      grid-row: 1 / 4;
      box-shadow: 0 0 1rem 1rem rgba(128,128,128,0.1);

      display: flex;
      flex-direction: column;
      justify-content: start;
      align-items: start;
      background: white;

    }

    #portfolio-team-content{
      grid-column: 5 / 7;
      grid-row: 2 / 3;    

      display: flex;
      flex-direction:  column;
      justify-content: start;
      align-items: start;
 
    }

    #portfolio-role-content{
      grid-column: 4 / 5;
      grid-row: 2 / 3;
      display: flex;
      flex-direction:  column;
      justify-content: start;
      align-items: start;

}


#portfolio-time-content{
      grid-column: 6 / 7;
      grid-row: 1 / 2;
      display: flex;
      flex-direction:  column;
      justify-content: start;
      align-items: start;

}

#portfolio-objective-content{
      grid-column: 4 / 6;
      grid-row: 1 / 2;
      display: flex;
      flex-direction:  column;
      justify-content: start;
      align-items: start;

}


#portfolio-overview{
  display:flex;
  gap: 2rem;
  margin-top: 4rem;
  margin-bottom: 4rem;
  h2 {
    text-align: center;
    width: 100%;
  }
}

#portfolio-time{
  height: 50%;
  grid-column: 3 / 4;
  grid-row: 2 / 3;
  text-align:center;
  font-size: 2rem;
 display: flex;
 flex-direction:  column;
 justify-content: space-around;
 align-items: start;

}

    #portfolio-challenge{
    
      padding: 2rem;
      width: 33.3%;


      ul{

        padding: 1rem;
      }
      
    }

    .portfolio-subsection-headers{
      font-size: 2rem;
      font-weight: 300;
      text-transform: uppercase;
      width: 100%;
      text-align: center;
    }

    #portfolio-solution{

      
      display: flex;
      flex-direction: column;
      justify-content: start;
      align-items: center;
      background-color: white;
      padding: 2rem;
      width: 33.3%;
      box-shadow: 0 0 1rem 1rem rgba(128,128,128,0.1);

      
      h2{
        color:black;
        text-align:center;
      }
      li{
        padding: 0;
        margin: 0;
      }

      ul{
        list-style-type: none;
        padding: 0;
        margin: 0;
        padding: 1rem;
      }
    }

    #portfolio-results{

      display: flex;
      flex-direction: column;
      justify-content: start;
      align-items: center;
      background: white;
      padding: 2rem;
      width: 33.3%;
      box-shadow: 0 0 1rem 1rem rgba(128,128,128,0.1);

      
      h2{
        color:black;
        text-align:center;
      }
      li{
        padding: 0;
        margin: 0;
      }
    }

  }
</style>

<!-- <style lang="scss" is:global>

.main {
    display:flex;
    flex-direction: column;
    align-items: center;
    max-width: 100svw;
  }


  .portfolio-content {
    display:flex;
    flex-direction: column;
    align-items: center;
    
  
  }
  
  .portfolio-image-container {
    border-radius: 2rem;  
    padding:0;
    margin: 0;
    
    &:first-child {
      margin-right:1rem;
    }
    &:last-child {
      margin-left:1rem;
    }

  img {
  border-radius: 2rem;  
  margin: 0;
  padding: 0;
  height: auto;
  width: 100%;

  }
}

.portfolio-image-list{
  width: 100svw;
  overflow-x: hidden;
}

  .portfolio-intro-text{
    font-size: 1.25rem;
    line-height: 150%;
  }
  .main-image {
    width: 100svw;
    height: 100%;
  }

  .thumbnail-image{
    width: 100%;
    height: auto;
    border-radius: 1rem 1rem 0 0;
  }
  

  

  .portfolio-category{
    background: #333;
    color:white;
    text-transform: uppercase;
    padding: 0.25rem 0.5rem;
    border-radius: 1rem;
    margin: 0;
    font-weight: 700;
    font-size: 0.75rem;
    margin-bottom: 2rem;
    margin-right: 0.5rem;
  }
  .portfolio-title {
    font-size: 1.75rem;
    margin: 0;
    padding: 0;
    padding:  1rem 0;
    text-transform: uppercase;
  }

  .portfolio-gallery{
    position:relative;
    display: flex;
    gap: 20px;
    justify-content: center;
    align-items: center;
    flex-basis: 100%;
    width: 100%;
  }

  .problem-statement{
    background: linear-gradient(to right, #02AAB0 0%, #00CDAC  51%, #02AAB0  100%);
    padding: 1rem;
    color: white;
  }
 
  @media only screen and (min-width: 768px) {

    .portfolio-content {
    padding: 1rem;
    width: 100%;
    max-width: 1440px;
    margin: auto;
    p{
      font-size: 1.2rem;
    }
    li{
      line-height: 150%;
      font-size: 1.2rem;
    }
  }
  
    .portfolio-category-container{
      padding-top:1rem;
      padding-bottom: 1rem;
    }
    .portfolio-subheaders{
      font-size: 1.25rem;
      font-weight: 800;
      text-transform: uppercase;
    }
  .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
  
  .portfolio-intro-content{
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    gap: 2rem;
    padding-top: 2rem;
  }

  .portfolio-title {
    font-size: 2rem;
  }
.portfolio-intro {
  display: inline-block;
  width: 40%;
  background:white;
  padding: 2rem;
  border-radius: 2rem;

}
.portfolio-objectives {
  display:inline-block;
  width: 60%;

}

.portfolio-image-container {
  border-radius: 2rem;  
  padding:0;
  margin: 0;
  width: 100%;

  
  &:first-child {
    margin-right:1rem;
  }
  &:last-child {
    margin-left:1rem;
  }

}
.portfolio-image-container img {
  border-radius: 2rem;  
  margin: 0;
  width: 100%;
  height: auto;


}
.portfolio-text-content {
  margin: 0;
  background:white;
  padding: 2rem;
  margin-top:2rem;
  border-radius: 2rem;  
}

.portfolio-image-section{
  width: 100%;
  display: flex;
  border-radius: 2rem;  
  padding: 0;
  margin: 0;
  flex-direction:row;


}

  .portfolio-deliverables {
  border-radius: 2rem;  
  width: 100%;
}

 
}
</style> -->

<script>
  
</script>