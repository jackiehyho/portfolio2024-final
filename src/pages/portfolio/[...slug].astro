---
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const portfolioEntries = await getCollection('portfolio');
  return  portfolioEntries.map(entry => ({
    params: { slug: entry.slug }, props: { entry },
  }));
}

import Layout from '../../layouts/Layout.astro';

const { entry } = Astro.props;
const { Content } = await entry.render();
import { Image } from 'astro:assets';

---
<Layout title={entry.data.title} description={entry.data.description}>
	<main class="main" slot="main">
        <Image class="main-image" src={entry.data.thumbnail} alt={entry.data.alt} />

        <div class="container">
          <ul class="blog-metadata-list">
            <li class="blog-metadata-item"><p class="blog-metadata"><strong>Project Duration:</strong><br />{entry.data.projectDuration}</p></li>
            <li class="blog-metadata-item"><p class="blog-metadata"><strong>Role: </strong><br />{entry.data.author}</p></li>
            <li class="blog-metadata-item"><p class="blog-metadata"><strong>Technology:</strong><br />{entry.data.technology}</p></li>
          </ul>
          <Content />
      
          </div>
	</main>
</Layout>
<style lang="scss">
  .main {
    display:flex;
    flex-direction: column;
    align-items: center;
    
  }

  .main a {
    color: red;
  }

  .container {
    padding: 1rem;
    width: 100%;
    max-width: 768px;


    h1 {
      font-size: 1.25rem;}
  }

  .main-image {
    max-width: 100svw;
    height: 100%;

  }

  .blog-metadata-list{
    display: flex;
    justify-content: center;
    flex-direction: column;
    margin: 0;
    padding: 0;
    width: 100%;
  }
  .blog-metadata-item {
    list-style-type: none;
    display:inline-block;
    padding:1rem;



  }

  .blog-metadata {
    text-align:center;
    font-size: .75rem;
  }



  
  @media only screen and (min-width: 768px) {
  .blog-metadata-list{
    display: flex;
    justify-content: center;
    flex-direction: row;
    margin: 0;
    padding: 0;
    width: 100%;
  }
    }

</style>